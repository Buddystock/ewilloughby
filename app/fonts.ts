import { M_PLUS_1, Smooch_Sans } from 'next/font/google'

export const smooch = Smooch_Sans({
  subsets: ['latin'],
  display: 'swap'
})

export const mplus1 = M_PLUS_1({
  subsets: ['latin'],
  display: 'swap'
})
